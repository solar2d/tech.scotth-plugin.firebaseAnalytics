Sources for the plugin `plugin.firebaseAnalytics`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.firebaseAnalytics" = {
            publisherId = "tech.scotth",
        },
    },
}
```
